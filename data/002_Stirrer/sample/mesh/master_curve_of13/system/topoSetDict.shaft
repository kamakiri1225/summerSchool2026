FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    shaftFaces;
        type    faceSet;
        action  new;
        source  patchToFace;
        patch   defaultFaces;
    }
    {
        name    shaftFaces;
        type    faceSet;
        action  subset;
        source  cylinderToFace;
        p1      (0 0 -0.000001);
        p2      (0 0 0.110001);
        radius  0.0031;
    }
);
