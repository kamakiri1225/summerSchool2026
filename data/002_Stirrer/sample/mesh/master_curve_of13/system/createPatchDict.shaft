FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}

pointSync false;

patches
(
    {
        name shaft;
        patchInfo
        {
            type wall;
            inGroups (wall);
        }
        constructFrom set;
        set shaftFaces;
    }
);
