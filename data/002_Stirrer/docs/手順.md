## モデルのコピー
cp -r $FOAM_TUTORIALS/mesh/blockMesh/sphere case
cd case
## メッシュのエクスポート
メッシュをmeshフォルダにコピー
cp -r ../../model/Mesh_1.unv .

## OpenFOAM形式に変換
ideasUnvToFoam Mesh_1.unv

## スケール変換
transformPoints -rotate-angle '((0 0 1) -30)'
transformPoints -scale '(0.001 0.001 0.001)'

## topoSetの設定
cp -r /usr/lib/openfoam/openfoam2512/tutorials/compressible/rhoSimpleFoam/aerofoilNACA0012/system/topoSetDict system/

topoSet -dict system/topoSetDict

## createBaffleによる壁面設定
createBafflesDict を作成しました。

4 つのバッフル壁面の対応：

バッフル名	faceZone
wall_wing	wingFaceZone
wall_wing2	wingFaceZone2
wall_circ_z015	circularFaceZone_z015
wall_circ_z035	circularFaceZone_z035
実行順序は topoSet → createBaffles -overwrite

## 羽を曲げる
cp -r $FOAM_TUTORIALS/mesh/moveDynamicMesh/SnakeRiverCanyon master_curve
cd ../master_curve
cp -r ../case/constant/polyMesh constant/

controlDictの変更
cp $FOAM_TUTORIALS/mesh/moveDynamicMesh/twistingColumn/system/* master_curve/system/

1秒後に希望のメッシュになるようにする
endTime         1;   // ここを変更

deltaT          0.1; // ここを変更


0.orig/pointDisplacementの変更
cp -r master_curve/0.orig master_curve/0
moveDynamicMesh 